import 'package:flutter/material.dart';

class ShooKeeper extends StatelessWidget {
  const ShooKeeper({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
